# 01.OpenClaw 容器环境统一路径标准（`.openclaw` 方案）

## 1. 文档目标

本标准用于在 **OpenClaw 容器环境** 中统一以下内容：

- 二进制文件放置位置
- 第三方插件放置位置
- npm 依赖安装位置
- 临时文件位置
- 持久化文件位置
- 环境变量 `NODE_PATH` 配置方式
- Agent 会话内遵循的路径判断规则

本标准适合：

- Docker / 容器化 OpenClaw 环境
- 以 `node` 用户运行的 OpenClaw
- 需要长期维护、可复制部署的实例
- 需要统一插件、依赖、缓存、数据目录的环境

---

## 2. 根路径约定

默认用户目录：

```bash
/home/node
```

OpenClaw 主目录统一为：

```bash
/home/node/.openclaw
```

核心原则：

- 所有用户侧可写内容统一收口到 `/home/node/.openclaw/`
- 不再混用 `/home/node/bin`、`/home/node/npm`、`/home/node/tmp`
- 内置目录只读，用户扩展目录可写
- 临时文件和持久文件必须分离

---

## 3. 标准路径定义

### 3.1 二进制文件目录

所有用户自行安装的可执行文件统一放在：

```bash
/home/node/.openclaw/bin
```

适用对象：

- ffmpeg
- 7zz
- 自定义脚本
- 各类 CLI 工具

---

### 3.2 第三方插件目录

所有用户安装的第三方插件统一放在：

```bash
/home/node/.openclaw/extensions/<plugin-id>/
```

示例：

```bash
/home/node/.openclaw/extensions/my-plugin/
```

---

### 3.3 npm 用户依赖目录

所有用户补装的 npm 依赖统一放在：

```bash
/home/node/.openclaw/npm/node_modules
```

npm 工作目录统一为：

```bash
/home/node/.openclaw/npm
```

安装命令统一为：

```bash
cd /home/node/.openclaw/npm
npm install <package-name> --prefix /home/node/.openclaw/npm
```

---

### 3.4 临时文件目录

所有临时生成、临时下载、临时测试用文件统一放在：

```bash
/home/node/.openclaw/workspace/tmp
```

例如：

- 测试文件
- 中间文件
- 下载缓存
- 待处理文件

---

### 3.5 持久保存文件目录

所有需要长期保留、明确要求“保存”的文件统一放在：

```bash
/home/node/.openclaw/workspace/data
```

例如：

- 正式导出结果
- 用户要求保存的文件
- 归档文件
- 后续还会继续使用的成果文件

---

### 3.6 通用运行数据目录

运行时通用数据建议放在：

```bash
/home/node/.openclaw/data
```

---

## 4. 内置目录处理原则

OpenClaw 内置扩展目录通常为：

```bash
/app/extensions/
```

该目录视为：

- 只读
- 平台内置
- 不直接修改

处理规则：

1. **不要修改 `/app/extensions/`**
2. 如果内置插件缺依赖，只补依赖，不改源码
3. 缺失依赖统一安装到：

```bash
/home/node/.openclaw/npm/node_modules
```

---

## 5. 容器环境推荐目录结构

```bash
/home/node/
└── .openclaw/
    ├── .profile
    ├── openclaw.json
    ├── bin/
    ├── npm/
    │   ├── package.json
    │   ├── package-lock.json
    │   └── node_modules/
    ├── extensions/
    │   └── <plugin-id>/
    ├── data/
    └── workspace/
        ├── AGENTS.md
        ├── tmp/
        └── data/
```

---

## 6. 初始化步骤

### 第一步：创建目录

```bash
mkdir -p /home/node/.openclaw/bin
mkdir -p /home/node/.openclaw/extensions
mkdir -p /home/node/.openclaw/npm/node_modules
mkdir -p /home/node/.openclaw/data
mkdir -p /home/node/.openclaw/workspace/tmp
mkdir -p /home/node/.openclaw/workspace/data
```

---

### 第二步：初始化 npm 工作目录

```bash
cat >/home/node/.openclaw/npm/package.json <<'EOF'
{
  "name": "openclaw-user-npm",
  "private": true,
  "description": "User-scoped npm packages for OpenClaw runtime"
}
EOF
```

---

### 第三步：写入环境变量

需要写入以下文件：

- `/home/node/.profile`
- `/home/node/.bashrc`
- `/home/node/.openclaw/.profile`

统一写入：

```bash
export NODE_PATH="$NODE_PATH:/home/node/.openclaw/bin"
export NODE_PATH="$NODE_PATH:/home/node/.openclaw/npm/node_modules"
```

---

### 第四步：当前 shell 临时生效

```bash
export NODE_PATH="$NODE_PATH:/home/node/.openclaw/bin:/home/node/.openclaw/npm/node_modules"
```

---

## 7. 安装规则

### 7.1 安装二进制文件

统一放到：

```bash
/home/node/.openclaw/bin
```

---

### 7.2 安装第三方插件

统一放到：

```bash
/home/node/.openclaw/extensions/<plugin-id>/
```

安装后在 `openclaw.json` 中启用。

---

### 7.3 安装 npm 依赖

统一使用：

```bash
cd /home/node/.openclaw/npm
npm install <package-name> --prefix /home/node/.openclaw/npm
```

---

### 7.4 内置插件缺依赖时

不要修改 `/app/extensions/`，仅补依赖：

```bash
cd /home/node/.openclaw/npm
npm install <package-name> --prefix /home/node/.openclaw/npm
```

---

## 8. 文件使用规则

### 临时文件

以下情况一律放：

```bash
/home/node/.openclaw/workspace/tmp
```

包括：

- 下载测试文件
- 中间处理文件
- 运行缓存
- 临时导出文件

---

### 持久文件

以下情况一律放：

```bash
/home/node/.openclaw/workspace/data
```

包括：

- 用户明确要求保存的文件
- 正式输出文件
- 长期使用文件
- 归档文件

---

## 9. AGENTS.md 推荐写法

建议在 `AGENTS.md` 中加入以下规则，确保每次会话自动遵循：

```md
## OpenClaw 文件路径标准

遵循以下路径规则：

- 二进制文件统一放置于 `/home/node/.openclaw/bin`
- 第三方插件统一放置于 `/home/node/.openclaw/extensions/<plugin-id>/`
- 缺失依赖统一安装到 `/home/node/.openclaw/npm/node_modules`
- 临时文件统一放置于 `/home/node/.openclaw/workspace/tmp`
- 需要保存的文件统一放置于 `/home/node/.openclaw/workspace/data`
- 内置代码目录 `/app/extensions/` 只读，不修改

核心原则：

1. 只读内置代码：`/app/extensions/`
2. npm 相关文件统一使用 `/home/node/.openclaw/npm/`
3. 用户依赖统一使用 `/home/node/.openclaw/npm/node_modules`
4. 环境变量需把 `/home/node/.openclaw/npm/node_modules` 加入 `NODE_PATH`
5. 临时文件放 `/home/node/.openclaw/workspace/tmp`
6. 保存文件放 `/home/node/.openclaw/workspace/data`
```

---

## 10. 验证步骤

### 10.1 验证环境变量

```bash
echo "$NODE_PATH"
```

应包含：

- `/home/node/.openclaw/bin`
- `/home/node/.openclaw/npm/node_modules`

---

### 10.2 验证 OpenClaw 状态

```bash
openclaw status
```

如果状态正常，说明路径标准已正确生效。

---

### 10.3 进一步验证（可选）

```bash
openclaw models list --plain
openclaw status --deep
```

---

## 11. 判断规则速查表

| 类型         | 判断依据                       | 标准路径                                         | 操作方式                                        |
| ------------ | ------------------------------ | ------------------------------------------------ | ----------------------------------------------- |
| 内置插件     | 代码在 `/app/extensions/`      | 依赖装到 `/home/node/.openclaw/npm/node_modules` | 只补依赖，不改源码                              |
| 第三方插件   | 代码在 `.openclaw/extensions/` | `/home/node/.openclaw/extensions/<plugin-id>/`   | 放入后在配置中启用                              |
| 二进制文件   | 可执行程序                     | `/home/node/.openclaw/bin`                       | 直接放置                                        |
| 临时文件     | 测试/下载/中间文件             | `/home/node/.openclaw/workspace/tmp`             | 直接放置                                        |
| 保存文件     | 需要长期保存                   | `/home/node/.openclaw/workspace/data`            | 直接放置                                        |
| npm 用户依赖 | 用户补装包                     | `/home/node/.openclaw/npm/node_modules`          | `npm install --prefix /home/node/.openclaw/npm` |

---

## 12. 核心结论

在 OpenClaw 容器环境中，应统一采用以下单一标准：

- 所有用户可写运行资源统一归拢到 `/home/node/.openclaw/`
- 不再混用 `/home/node/bin`、`/home/node/npm`、`/home/node/tmp`
- 内置目录只读
- 临时文件与持久文件分离
- 插件目录与依赖目录分离
- 通过 `NODE_PATH` 和 `AGENTS.md` 固化规则
