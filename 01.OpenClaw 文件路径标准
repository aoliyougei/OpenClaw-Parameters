# OpenClaw 容器环境补充路径规范（2026.3.22+）

## 1. 文档目的

本文档用于补充说明 OpenClaw `2026.3.22+` 容器环境下，仍需显式统一或容易混用的路径规则。

对于当前版本中已经默认采用、且环境内通常已存在的标准路径，不再重复展开说明。本文档仅保留以下内容：

- 需要补齐的目录规范
- 仍建议显式设置的环境变量
- 容器内置目录与用户目录边界
- 安装与文件落盘规则
- AGENTS.md 推荐长期规则
- 验证方法

---

## 2. 基本原则

OpenClaw 容器环境继续遵循以下单一原则：

- 所有用户可写资源统一归拢到：

```bash
/home/node/.openclaw
```

- `/app/...` 视为镜像内置目录，默认只读
- 用户扩展、用户依赖、用户二进制、运行数据、临时文件、持久文件应彼此分离
- 路径控制统一使用 `OPENCLAW_*`
- 不再使用 `.moltbot`、`moltbot.json`、`CLAWDBOT_*`、`MOLTBOT_*` 旧约定

---

## 3. 需要显式统一的补充目录

以下目录建议在容器环境中显式创建并长期保留。

### 3.1 用户二进制目录

```bash
/home/node/.openclaw/bin
```

用于放置用户自行补装的 CLI、脚本或辅助工具。

### 3.2 通用运行数据目录

```bash
/home/node/.openclaw/data
```

用于放置不属于 workspace 文档、但需要长期保留的运行数据。

### 3.3 用户 npm 依赖目录

```bash
/home/node/.openclaw/npm
/home/node/.openclaw/npm/node_modules
```

用于安装用户级 Node.js 依赖，避免改动 `/app` 内置内容。

### 3.4 临时文件目录

```bash
/home/node/.openclaw/workspace/tmp
```

用于：

- 临时下载文件
- 中间处理文件
- 调试产物
- 一次性导出文件

### 3.5 持久文件目录

```bash
/home/node/.openclaw/workspace/data
```

用于：

- 用户要求长期保存的文件
- 正式导出文件
- 归档文件
- 后续仍需继续使用的成果文件

### 3.6 定时任务目录

```bash
/home/node/.openclaw/cron
```

用于放置后续需要持久化管理的定时任务相关内容。

---

## 4. 环境变量规则

建议在容器启动脚本、Compose `environment` 或用户 shell 环境中显式设置：

```bash
export OPENCLAW_STATE_DIR=/home/node/.openclaw
export OPENCLAW_CONFIG_PATH=/home/node/.openclaw/openclaw.json
export PATH="/home/node/.openclaw/bin:$PATH"
export NODE_PATH="/home/node/.openclaw/npm/node_modules${NODE_PATH:+:$NODE_PATH}"
```

### 说明

- `PATH` 只负责查找二进制
- `NODE_PATH` 只负责查找 Node.js 模块
- 不建议把 `bin` 写入 `NODE_PATH`

---

## 5. 内置目录与用户目录边界

容器内常见内置目录：

```bash
/app
```

处理原则：

1. `/app/...` 默认视为只读
2. 不直接修改内置目录源码
3. 用户自定义内容统一写入 `/home/node/.openclaw/...`
4. 内置插件缺依赖时，优先在用户 npm 目录补依赖

---

## 6. 安装与使用规则

### 6.1 安装用户二进制

统一放入：

```bash
/home/node/.openclaw/bin
```

### 6.2 安装第三方插件

统一放入：

```bash
/home/node/.openclaw/extensions/<plugin-id>/
```

### 6.3 安装用户 npm 依赖

统一在：

```bash
/home/node/.openclaw/npm
```

执行方式：

```bash
cd /home/node/.openclaw/npm
npm install <package-name> --prefix /home/node/.openclaw/npm
```

### 6.4 内置插件缺依赖时

不要修改 `/app/extensions/`，只补用户依赖：

```bash
cd /home/node/.openclaw/npm
npm install <package-name> --prefix /home/node/.openclaw/npm
```

---

## 7. 文件落盘规则

### 7.1 临时文件

统一放入：

```bash
/home/node/.openclaw/workspace/tmp
```

### 7.2 持久文件

统一放入：

```bash
/home/node/.openclaw/workspace/data
```

规则：

- 临时文件与持久文件必须分离
- 需要长期保留的结果，不应写入 `tmp`
- 一次性处理产物，不应混入 `data`

---

## 8. AGENTS.md 推荐补充规则

建议在 workspace 的 `AGENTS.md` 中补充：

```md
## OpenClaw 容器标准路径补充规则

遵循以下补充规范：

- 用户二进制：`/home/node/.openclaw/bin`
- 用户 npm 依赖：`/home/node/.openclaw/npm/node_modules`
- 通用运行数据：`/home/node/.openclaw/data`
- 临时文件：`/home/node/.openclaw/workspace/tmp`
- 持久文件：`/home/node/.openclaw/workspace/data`
- 定时任务目录：`/home/node/.openclaw/cron`

规则：

1. `/app/...` 视为内置只读目录，不直接修改
2. 所有用户可写资源统一归拢到 `/home/node/.openclaw/`
3. 二进制走 `PATH`，Node 模块走 `NODE_PATH`
4. 临时文件与持久文件必须分离
5. 统一使用 `OPENCLAW_STATE_DIR` / `OPENCLAW_CONFIG_PATH`
6. 不再使用 `.moltbot`、`moltbot.json`、`CLAWDBOT_*`、`MOLTBOT_*` 旧约定
```

---

## 9. 验证方法

### 9.1 验证环境变量

```bash
echo "$OPENCLAW_STATE_DIR"
echo "$OPENCLAW_CONFIG_PATH"
echo "$PATH"
echo "$NODE_PATH"
```

### 9.2 验证目录

```bash
ls -ld /home/node/.openclaw/bin
ls -ld /home/node/.openclaw/data
ls -ld /home/node/.openclaw/npm/node_modules
ls -ld /home/node/.openclaw/workspace/tmp
ls -ld /home/node/.openclaw/workspace/data
ls -ld /home/node/.openclaw/cron
```

### 9.3 验证运行状态

```bash
openclaw gateway status
openclaw status
```

---

## 10. 核心结论

在 OpenClaw `2026.3.22+` 容器环境中：

- 已存在的标准路径不再重复展开
- 仅对容易缺失、容易混用的目录和环境变量做补充规范
- 所有用户可写资源继续统一归拢到：

```bash
/home/node/.openclaw
```

- 用户二进制、用户依赖、运行数据、临时文件、持久文件应长期分目录管理
- 新增内容优先写入用户目录，不修改 `/app` 内置目录
