# OpenClaw 启用向量记忆、缓存与混合搜索操作指南（SiliconFlow / Qwen3-Embedding-8B）

## 目标

本文档用于在 OpenClaw 中启用以下能力：

- 向量记忆检索（Vector Search）
- 索引缓存 / 文件监听增量更新（Watch + Cache）
- 混合搜索（BM25 + Vector Hybrid Search）

本文采用：

- 向量模型：`Qwen/Qwen3-Embedding-8B`
- OpenAI-compatible API Base URL：`https://api.siliconflow.cn/v1`

> 说明：OpenClaw 官方 memory 配置支持使用 `provider: "openai"` 对接自定义 OpenAI-compatible embeddings endpoint。

---

## 一、前置条件

1. OpenClaw 已正常运行
2. 可访问 OpenClaw 主配置文件（常见路径：`~/.openclaw/openclaw.json`）
3. 已持有 SiliconFlow API Key
4. OpenClaw 版本支持 `agents.defaults.memorySearch` 配置

---

## 二、推荐配置

将以下配置写入 OpenClaw 主配置文件中的：

- `agents.defaults.memorySearch`

推荐配置示例：

```json
{
  "agents": {
    "defaults": {
      "memorySearch": {
        "enabled": true,
        "provider": "openai",
        "model": "Qwen/Qwen3-Embedding-8B",
        "remote": {
          "baseUrl": "https://api.siliconflow.cn/v1",
          "apiKey": "${EMBEDDING_API_KEY}"
        },
        "sync": {
          "watch": true
        },
        "query": {
          "hybrid": {
            "enabled": true,
            "vectorWeight": 0.6,
            "textWeight": 0.4,
            "candidateMultiplier": 3
          }
        }
      }
    }
  }
}
```

---

## 三、配置项说明

### 1. `enabled: true`

开启 memory search。

### 2. `provider: "openai"`

因为 SiliconFlow 提供的是 OpenAI-compatible 接口，所以这里使用 `openai`。

### 3. `model: "Qwen/Qwen3-Embedding-8B"`

指定用于 embedding 的向量模型。

### 4. `remote.baseUrl`

填写 SiliconFlow 的 OpenAI-compatible API 地址：

```text
https://api.siliconflow.cn/v1
```

### 5. `remote.apiKey`

已配置环境变量：EMBEDDING_API_KEY。

### 6. `sync.watch: true`

开启文件监听与索引增量更新。
这会让 `MEMORY.md` 和 `memory/**/*.md` 更新后自动标记索引 dirty，并在后续搜索/同步时刷新。

### 7. `query.hybrid.enabled: true`

开启混合搜索。
混合搜索会将：

- 向量相似度（语义匹配）
- BM25 全文关键词匹配（精确 token / 配置项 / 版本号）

组合在一起，比纯向量更适合 OpenClaw 这类技术文档/配置/版本问题场景。

### 8. `vectorWeight` / `textWeight`

推荐初始值：

- `vectorWeight: 0.6`
- `textWeight: 0.4`

含义：

- 语义相关性略高于关键词匹配
- 同时保留版本号、配置名、命令名等精确命中能力

### 9. `candidateMultiplier: 3`

候选池倍率，先取一个稳妥值即可。

- 太小：召回不足
- 太大：开销变高

---

## 四、修改完成后需要做什么

修改配置后，需要让 OpenClaw 重新加载配置。
常见方式：

```bash
openclaw gateway restart
```

如果你不确定环境命令是否一致，也可以使用：

```bash
openclaw gateway status
```

确认当前网关状态。

---

## 五、功能测试方法

建议按以下顺序测试。

### 测试 1：基础配置是否生效

目标：确认 OpenClaw 没有因为 memory 配置报错。

检查点：

- gateway 可正常启动
- 没有因 `memorySearch` 配置报 schema / provider / model 错误
- 没有因 API 认证失败导致启动异常

如果有日志系统，重点看：

- memory search 初始化日志
- embedding provider 初始化日志
- hybrid / FTS 初始化日志

---

### 测试 2：向量检索是否生效

目标：确认 embedding 真的在工作。

#### 准备测试内容

在 workspace 中准备：

`MEMORY.md`

```md
HanMing 更关注 OpenClaw 版本更新的迁移影响和稳定性。
```

然后发起语义相关但不完全同词的查询，例如：

- “升级代价大不大？”
- “这次更新对现有部署影响如何？”
- “我更关心 release 的什么部分？”

#### 预期结果

如果向量检索生效，应能召回上面这条记忆，即使查询没有直接出现“迁移影响”和“稳定性”原词。

---

### 测试 3：关键词/BM25 是否参与混合搜索

目标：确认 hybrid 不是纯向量。

#### 准备测试内容

在 memory 文件中写入包含精确关键词的信息，例如：

```md
OpenClaw v2026.3.22 移除了 browser.relayBindHost，并废弃 legacy Chrome extension relay。
```

然后搜索：

- `browser.relayBindHost`
- `v2026.3.22`
- `legacy Chrome extension relay`

#### 预期结果

如果 hybrid 生效，这类精确 token、版本号、配置项应该能被稳定命中。

---

### 测试 4：缓存 / 文件监听 / 增量更新是否生效

目标：确认 watch + cache 正常工作。

#### 步骤

1. 先写入一条旧内容
2. 触发一次 memory 搜索
3. 修改该条内容或新增一条新内容
4. 再次触发 memory 搜索

#### 预期结果

- 新内容能在后续搜索中出现
- 不需要手动清空整个索引
- 不需要每次全量重建

> 官方文档说明：memory 文件变化会被 watcher 标记为 dirty，并以 debounce 方式同步。

---

## 六、推荐测试样例

### 样例 A：语义测试

记忆：

```md
HanMing 更关心 breaking changes、迁移成本和稳定性，而不是只看 changelog。
```

查询：

- “我看版本更新时最在意什么？”
- “我更想知道哪些更新信息？”

预期：

- 能召回这条记忆

### 样例 B：精确关键词测试

记忆：

```md
OpenClaw memory 配置路径是 agents.defaults.memorySearch。
```

查询：

- `agents.defaults.memorySearch`

预期：

- 精确命中

### 样例 C：更新后增量测试

旧记忆：

```md
HanMing 偏好简短总结。
```

改成：

```md
HanMing 偏好结构化、信息密度高的总结。
```

查询：

- “HanMing 喜欢什么样的回答风格？”

预期：

- 新内容应优先生效

---

## 七、常见问题与排查

### 1. 配置写了但没有结果

优先检查：

- `memorySearch.enabled` 是否为 `true`
- `provider` 是否为 `openai`
- `model` 是否写对
- `baseUrl` 是否可达
- `apiKey` 是否有效

### 2. 语义问题查不到，只有精确词能查到

可能原因：

- embedding 接口没有真正生效
- API key 无效
- 模型名不可用
- baseUrl 不正确

### 3. 精确 token 查不到

可能原因：

- hybrid 未开启
- 平台上全文检索不可用，退回到了纯向量模式
- 记忆内容没有落到被索引的 Markdown 文件里

### 4. 修改 memory 后搜索结果不更新

可能原因：

- gateway 还没重载新配置
- watcher 尚未完成 debounce 后同步
- 当前搜索命中了旧索引缓存

处理建议：

- 重启 gateway
- 稍等数秒后再次搜索
- 确认修改的是 `MEMORY.md` 或 `memory/**/*.md`

---

## 八、生产环境建议

### 建议 1：不要把 API Key 长期明文写进配置

更推荐：

- 环境变量
- SecretRef
- 受控密钥管理方式

### 建议 2：先用内置 hybrid，不要一上来就上 QMD

OpenClaw 内置的 memorySearch 已支持：

- 向量搜索
- 混合搜索
- watcher / debounce / 缓存索引

这已经足够覆盖多数场景。
QMD 更强，但复杂度更高，适合后续再增强。

### 建议 3：记忆内容要写得结构化

即使检索能力开得再强，记忆内容本身如果全是噪音，效果也不会好。
建议搭配：

- `MEMORY.md`：长期记忆
- `memory/YYYY-MM-DD.md`：日常记录

---

## 九、推荐结论

对于 OpenClaw 的技术型使用场景，推荐默认开启：

- 向量检索：**开**
- 缓存 / 文件监听更新：**开**
- 混合搜索：**开**

推荐初始组合：

- provider: `openai`
- model: `Qwen/Qwen3-Embedding-8B`
- baseUrl: `https://api.siliconflow.cn/v1`
- hybrid: enabled
- watch: enabled

这套组合在“语义理解 + 精确关键词命中 + 日常增量更新”之间比较平衡，适合大多数 OpenClaw 记忆使用场景。
