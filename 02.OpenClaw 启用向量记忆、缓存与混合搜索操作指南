# OpenClaw 安装与记忆检索配置操作文档

本文档只关注 **OpenClaw 安装后的记忆检索配置与验证**，不再展开标准路径规则安装内容。

适用目标：

- 在一个已经完成基础安装的 OpenClaw 环境中
- 启用向量记忆检索、缓存、文件监听、混合搜索
- 创建并准备记忆文件
- 完成首轮 memory 索引构建
- 验证语义召回、关键词命中、缓存与混合搜索是否生效
- 在出现索引异常时完成修复

---

## 一、前置条件

开始前默认以下条件已经满足：

1. OpenClaw 已安装并可运行
2. 主配置文件存在：

```text
/home/node/.openclaw/openclaw.json
```

3. workspace 已存在：

```text
/home/node/.openclaw/workspace
```

4. 已具备可用的 embedding API Key

如果你还没有完成标准路径初始化，请先参考“标准路径规则安装配置文档”，不要把路径初始化步骤混在本文件中执行。

---

## 二、记忆检索配置目标

本次配置目标为：

- 启用向量记忆检索
- 启用 embedding 缓存
- 启用文件监听增量更新
- 启用混合搜索（vector + FTS）
- 使用 OpenAI-compatible 远程 embedding 接口

推荐组合：

- provider: `openai`
- model: `Qwen/Qwen3-Embedding-8B`
- remote.baseUrl: `https://api.siliconflow.cn/v1`
- remote.apiKey: `${EMBEDDING_API_KEY}`

---

## 三、配置写入位置

记忆检索配置必须写在：

```json
agents.defaults.memorySearch
```

不要错误写到顶层 `memorySearch`。

---

## 四、推荐配置内容

在 `/home/node/.openclaw/openclaw.json` 中确保存在以下结构：

```json
{
  "agents": {
    "defaults": {
      "memorySearch": {
        "enabled": true,
        "provider": "openai",
        "model": "Qwen/Qwen3-Embedding-8B",
        "remote": {
          "baseUrl": "https://api.siliconflow.cn/v1",
          "apiKey": "${EMBEDDING_API_KEY}"
        },
        "sync": {
          "watch": true
        },
        "cache": {
          "enabled": true
        },
        "query": {
          "hybrid": {
            "enabled": true,
            "vectorWeight": 0.6,
            "textWeight": 0.4,
            "candidateMultiplier": 3
          }
        }
      }
    }
  }
}
```

字段说明：

- `enabled = true`：启用 memorySearch
- `provider = "openai"`：用于 OpenAI-compatible 接口
- `model = "Qwen/Qwen3-Embedding-8B"`：指定 embedding 模型
- `remote.baseUrl`：远程 embedding 服务地址
- `remote.apiKey`：通过环境变量引用 API Key
- `sync.watch = true`：启用 watcher
- `cache.enabled = true`：启用缓存
- `hybrid.enabled = true`：启用混合搜索

---

## 五、环境变量要求

确保 OpenClaw 运行环境中可用：

```bash
export EMBEDDING_API_KEY=你的_embedding_api_key
```

如果是通过容器、supervisor 或其他进程管理方式运行 OpenClaw，请确保该变量对实际运行 gateway 的进程可见。

---

## 六、准备记忆文件

### 1. 创建长期记忆文件

如果不存在：

```bash
touch /home/node/.openclaw/workspace/MEMORY.md
```

### 2. 创建 daily memory 目录

```bash
mkdir -p /home/node/.openclaw/workspace/memory
```

说明：

- 这一步必须做
- 即使暂时没有 daily notes，也建议先建目录
- 否则后续扫描可能提示 `memory directory missing`

### 3. 写入最小测试内容

```bash
cat > /home/node/.openclaw/workspace/MEMORY.md <<'EOF'
HanMing 更关心 breaking changes、迁移成本和稳定性，而不是只看 changelog。

OpenClaw memory 配置路径是 agents.defaults.memorySearch。

OpenClaw v2026.3.22 移除了 browser.relayBindHost，并废弃 legacy Chrome extension relay。
EOF
```

---

## 七、配置生效方式

修改完配置后，需要让运行中的 OpenClaw 读取新配置。

### 方式 A：如果环境支持标准 gateway 管理

```bash
openclaw gateway restart
```

### 方式 B：如果在容器环境中运行且不支持 systemd/user service

需要重启真正运行 OpenClaw 的进程或容器，而不是单纯依赖：

```bash
openclaw gateway restart
```

例如：

- 重启容器
- 重启 supervisor 管理的 OpenClaw 进程
- 重启 entrypoint 启动的前台 gateway 进程

---

## 八、配置后的状态检查

### 1. 总体状态

```bash
openclaw status
```

理想状态应包含：

- `vector ready`
- `fts ready`
- `cache on`

### 2. 官方 memory 深度状态

```bash
openclaw memory status --agent main --deep --json
```

重点检查字段：

- `files`
- `chunks`
- `dirty`
- `provider`
- `model`
- `cache.enabled`
- `cache.entries`
- `fts.available`
- `vector.available`
- `embeddingProbe.ok`

理想状态示例：

- `provider = openai`
- `model = Qwen/Qwen3-Embedding-8B`
- `embeddingProbe.ok = true`
- `fts.available = true`
- `vector.available = true`
- `files >= 1`
- `chunks >= 1`

---

## 九、如果索引未完成：官方强制重建

如果出现以下现象：

- `memory_search` 返回 `database is not open`
- `openclaw status` 长时间显示 `0 files / 0 chunks / dirty`
- 配置已生效但查不到任何内容

优先执行官方重建：

```bash
openclaw memory index --agent main --force --verbose
```

然后再次检查：

```bash
openclaw memory status --agent main --deep --json
```

本次实际验证表明，这一步是修复“配置已生效但索引未稳定完成首轮构建”的关键动作。

---

## 十、检索验证步骤

### 1. 语义召回验证

```bash
openclaw memory search "我看版本更新时最在意什么？"
```

期望命中：

- `HanMing 更关心 breaking changes、迁移成本和稳定性...`

### 2. 精确关键词验证

```bash
openclaw memory search "agents.defaults.memorySearch"
openclaw memory search "browser.relayBindHost"
```

期望命中对应 `MEMORY.md` 内容。

### 3. 混合搜索验证

若结果中可见：

- `provider = openai`
- `mode = hybrid`

则可认定混合搜索已生效。

### 4. 缓存验证

执行：

```bash
openclaw memory status --agent main --deep --json
```

确认：

- `cache.enabled = true`
- `cache.entries >= 1`

### 5. 文件监听验证

向 `MEMORY.md` 或 `workspace/memory/*.md` 追加内容后再次检索。

例如：

```bash
echo '' >> /home/node/.openclaw/workspace/MEMORY.md
echo 'HanMing 安装 OpenClaw 时优先关注稳定性和迁移成本。' >> /home/node/.openclaw/workspace/MEMORY.md
```

然后执行：

```bash
openclaw memory search "安装 OpenClaw 时优先关注什么？"
```

如果未及时命中，可再次执行：

```bash
openclaw memory index --agent main --force --verbose
```

---

## 十一、本次故障复盘

### 1. 初始现象

曾出现：

- `memory_search` 返回 `provider: none`
- 仅 `fts-only`
- 查询无结果
- `database is not open`
- `openclaw status` 显示 `0 files / 0 chunks / dirty`

### 2. 根因

根因不是 API Key 缺失，也不是 provider 配置错误，而是：

- memorySearch 配置启用后，索引没有稳定完成首轮同步
- 数据库文件虽已创建，但 file/chunk 状态没有正确收敛
- 查询在索引未完全准备好时触发，导致出现 `database is not open`

### 3. 有效修复措施

按以下顺序修复成功：

1. 正确写入 `agents.defaults.memorySearch`
2. 确认 provider/model/baseUrl/apiKey/watch/cache/hybrid 配置齐全
3. 重启或重载 gateway
4. 创建 `workspace/memory/` 目录
5. 使用官方命令强制重建索引：

```bash
openclaw memory index --agent main --force --verbose
```

### 4. 结论

如果配置已正确，但 memory 检索仍异常，优先怀疑：

- 首轮索引未完成
- 数据库状态不稳定
- 需要官方 reindex 收口

不要第一时间误判为：

- API Key 错
- provider 不可用
- 文件不存在
- 路径错误

应优先执行：

```bash
openclaw memory status --agent main --deep --json
openclaw memory index --agent main --force --verbose
```

---

## 十二、推荐收尾动作

### 1. 修复配置文件权限

```bash
chmod 600 /home/node/.openclaw/openclaw.json
```

### 2. 再次检查状态

```bash
openclaw status
openclaw memory status --agent main --deep --json
```

### 3. 再做一次语义检索

```bash
openclaw memory search "我看版本更新时最在意什么？"
```

---

## 十三、最小可执行流程

在已经完成基础安装的 OpenClaw 环境中，推荐直接按以下顺序操作：

```bash
mkdir -p /home/node/.openclaw/workspace/memory

touch /home/node/.openclaw/workspace/MEMORY.md

cat > /home/node/.openclaw/workspace/MEMORY.md <<'EOF'
HanMing 更关心 breaking changes、迁移成本和稳定性，而不是只看 changelog。

OpenClaw memory 配置路径是 agents.defaults.memorySearch。

OpenClaw v2026.3.22 移除了 browser.relayBindHost，并废弃 legacy Chrome extension relay。
EOF
```

然后确保 `openclaw.json` 中存在：

- `agents.defaults.memorySearch.enabled = true`
- `provider = openai`
- `model = Qwen/Qwen3-Embedding-8B`
- `remote.baseUrl = https://api.siliconflow.cn/v1`
- `remote.apiKey = ${EMBEDDING_API_KEY}`
- `sync.watch = true`
- `cache.enabled = true`
- `query.hybrid.enabled = true`

然后执行：

```bash
openclaw memory status --agent main --deep --json
openclaw memory index --agent main --force --verbose
openclaw memory search "我看版本更新时最在意什么？"
openclaw memory search "agents.defaults.memorySearch"
openclaw memory search "browser.relayBindHost"
chmod 600 /home/node/.openclaw/openclaw.json
```

---

## 十四、完成判定标准

满足以下条件即可判定记忆检索配置完成：

1. `openclaw memory status --agent main --deep --json` 中：
   - `provider = openai`
   - `embeddingProbe.ok = true`
   - `fts.available = true`
   - `vector.available = true`
   - `files >= 1`
   - `chunks >= 1`

2. `openclaw memory search` 能搜到 `MEMORY.md` 中的内容

3. `workspace/memory/` 目录已存在

4. `openclaw.json` 权限已收紧为 `600`

---

以上即为仅针对“安装后的记忆检索配置、验证与修复”的操作文档。
